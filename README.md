# F1-Race-Predictor-V02
Predicts the results of an F1 Race

Uses a Random Forest model trained on historical F1 race data to predict the finishing order for upcoming races. It pulls race and qualifying data directly from the FastF1 API, engineers meaningful features from that data, and outputs a ranked predicted finishing order for any given race weekend.
